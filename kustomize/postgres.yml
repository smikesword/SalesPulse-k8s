apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: inventory-service-db
spec:
  instances: 1

  bootstrap:
    initdb:
      database: yodb_inventory
      owner: user
    

  storage:
    size: 1Gi